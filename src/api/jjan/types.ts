export type AuthResponseData = {
  email: string;
  nickName: string;
  profile?: string;
  address: string;
  gender: string;
  birth: string;
  drinkCapacity: string;
};

export type SigninData = {
  email: string;
  password: string;
};
